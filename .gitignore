# Environment — never commit real secrets
.env
.env.local
.env.*.local

# ChromaDB data directories (if checked out locally for inspection)
chroma-data/
data/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Cursor local state (plans are in .cursor/plans/ which is user-local)
.cursor/plans/

# Misc
*.log
